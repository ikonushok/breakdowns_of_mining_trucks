# Классификация выбросов в телеметрии Komatsu PC4000

## ✅ Краткая классификация выбросов (на основе рассчитанных порогов)

---

## 1. `speed_gps_mean` — **real_anomaly** (706 шт.)

**Почему:**

Экскаватор действительно может:
- резко ускориться на 1–3 км/ч  
- двигаться платформой, делать рывки  

Все выбросы лежат в пределах **физически возможных скоростей (до 5–10 км/ч)**.  
Никаких «150 км/ч» нет → значит, это реальные скачки движения.

**→ Использовать в моделях как реальные аномалии.**

---

## 2. `speed_mean_60s` / `speed_std_60s` — **real_anomaly**

Резкие изменения скорости в окне 60 секунд отражают:
- удары ковша  
- неровности  
- динамические циклы копания  

Это реальные физические эффекты.

**→ real_anomaly**

---

## 3. `speed_diff` / `speed_gps_diff_5s` — **real_anomaly**

Большинство скачков скорости находятся в диапазоне:

**−2…+4 км/ч за 5 секунд** — это нормально для экскаватора.

Только **1 выброс** классифицируется как ошибка GPS.

→ Почти все — реальные динамические изменения.

---

## 4. `payload_last_trip` — **real_anomaly**

Загрузка самосвалов:
- меняется от рейса к рейсу  
- зависит от материала, глубины копания и объёма ковша  

Выбросы отражают реальные изменения массы груза.

**→ Оставляем. Это важный операционный признак.**

---

# ❌ Ошибки датчиков (`sensor_error`)

Ниже признаки, чьи выбросы находятся **за пределами физически возможных значений**.  
Это артефакты IMU, переполнение регистров или GPS-jump.

---

## 5. `inclinom_platx_mean` / `inclinom_platy_mean` — **sensor_error**

IMU выдаёт выбросы:
- сотни  
- тысячи  
- десятки тысяч  

Реальный крен платформы экскаватора **0–20°**, максимум **±35°**.  
Все, что за пределами → артефакт.

---

## 6. `inclinom_arm_mean` / `inclinom_boomx_mean` — **sensor_error**

Стрела и рукоять:
- работают в пределах **0–120°**

Выбросы → **несколько тысяч** градусов → невозможны.

---

## 7. `boom_angle_delta_5s` / `arm_angle_delta_5s` — **sensor_error**

Таблица показывает **0 реальных аномалий**.

Почему?

Потому что выбросы:
- огромные (тысячи)
- лежат за пределами возможного изменения угла (0–10° за 5 сек)

**Это переполнение IMU/ошибка датчика.**

---

## 8. `platform_tilt_magnitude` — **sensor_error**

Нормальный крен:
- **0–20°**

В данных:
- выбросы десятки тысяч

**→ артефакт IMU**

---

## 9. `boom_vibration_std_60s` / `arm_vibration_std_60s` — **sensor_error**

Нормальная вибрация:
- ±1–5°

В данных:
- огромные значения → IMU overflow

---

## 10. `boom_angle_diff` / `arm_angle_diff` — **sensor_error**

Физически возможные изменения:
- **±0–5°**

В выбросах → тысячи ° → нереально.

---

## 11. `lon` / `lat` — **sensor_error**

990 выбросов — это:
- GPS jumps  
- координаты 0  
- потеря спутников  

---

## 12. `alt` — **sensor_error**

Высота не может быть отрицательной.  
Выбросы → ошибка GPS.

---

# 📌 Итог

## 🔥 Оставляем (реальные аномалии):
- `speed_gps_mean`
- `speed_mean_60s`
- `speed_std_60s`
- `speed_diff`
- `speed_gps_diff_5s`
- `payload_last_trip`

## 🛑 Удаляем / фильтруем (ошибки датчиков):
- всё, что начинается с `inclinom_`
- `platform_tilt_magnitude`
- все `*_vibration_std_*`
- все `*_angle_delta_*`, `*_angle_diff_*`, `*_roc_deg_*`
- `lon`, `lat`, `alt`

---

Готово. Можно вставлять в ноутбук или использовать как документацию.
